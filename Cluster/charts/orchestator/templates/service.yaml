apiVersion: v1
kind: Service
metadata:
  name: {{ template "orchestator.fullname" . }}
  labels:
    app: {{ template "orchestator.name" . }}
    chart: {{ template "orchestator.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "orchestator.name" . }}
    release: {{ .Release.Name }}
